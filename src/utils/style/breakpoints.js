// Defining breakpoints for responsive design
const size = {
	laptop: '920px',
}

// Media queries for different device sizes
export const device = {
    laptop: `min-width: ${size.laptop}`, // Media query for laptopSmall
}
